# Deterministic class order (alphabetical): com.lattex.api generators write the
# examples/*.html pages BEFORE com.lattex.harness browser tests read them, so a
# full suite run always exercises the CURRENT runtime, never a stale committed page.
junit.jupiter.testclass.order.default=org.junit.jupiter.api.ClassOrderer$ClassName
